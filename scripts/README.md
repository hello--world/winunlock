# 自动化构建脚本说明

## 📋 可用脚本

### 1. `wait-for-build.js` - 等待并检测构建结果

等待 GitHub Actions 构建完成并返回结果。

**使用方法：**
```bash
# 等待最新提交的构建
npm run wait-build

# 或指定提交 SHA
node scripts/wait-for-build.js <commit_sha>
```

**功能：**
- ✅ 自动等待构建完成
- ✅ 定期检查构建状态（每30秒）
- ✅ 显示状态变化
- ✅ 返回构建结果（成功/失败）
- ✅ 如果失败，提供错误 URL

### 2. `auto-build-and-wait.js` - 完全自动化构建

提交代码 → 推送 → 等待构建 → 检查结果 → 自动修复循环

**使用方法：**
```bash
npm run auto-build-wait "你的提交信息"
```

**功能：**
- ✅ 自动提交代码
- ✅ 自动推送
- ✅ 自动等待构建完成
- ✅ 自动检查构建状态
- ✅ 如果失败，提示修复
- ✅ 重复直到成功

### 3. `check-workflow.js` - 检查工作流程状态

检查 GitHub Actions 工作流程的运行状态。

**使用方法：**
```bash
node scripts/check-workflow.js
```

### 4. `get-workflow-logs.js` - 获取构建日志

获取工作流程运行的详细日志。

**使用方法：**
```bash
node scripts/get-workflow-logs.js <run_id>
```

## 🔧 配置

### 设置 GitHub Token（推荐）

```bash
export GITHUB_TOKEN=your_github_personal_access_token
```

获取 Token: https://github.com/settings/tokens/new
需要权限：`repo`, `actions:read`

### 调整检查间隔

编辑脚本中的 `CONFIG`：
```javascript
checkInterval: 30000,  // 30秒
maxWaitTime: 600000,   // 10分钟
```

## 📊 使用示例

### 示例 1: 等待构建完成

```bash
# 提交并推送代码
git add -A
git commit -m "修复编译错误"
git push origin dev

# 等待构建完成
npm run wait-build
```

### 示例 2: 完全自动化

```bash
# 一键完成：提交 → 推送 → 等待 → 检查
npm run auto-build-wait "修复编译错误"
```

### 示例 3: 在 Cursor 中使用

在 Cursor 中，你可以结合这些脚本使用：

```bash
# 1. 提交代码
git add -A && git commit -m "修复错误" && git push origin dev

# 2. 在 Cursor 中说：
"等待构建完成并检查结果"

# 3. AI 会自动运行：
npm run wait-build
```

## 🎯 完全自动化流程

```bash
# 方式 1: 使用脚本
npm run auto-build-wait "你的提交信息"

# 方式 2: 在 Cursor 中
"完全自动化构建直到成功"
```

## ⚙️ 环境变量

- `GITHUB_TOKEN` - GitHub Personal Access Token（可选但推荐）

## 📝 注意事项

- 脚本需要 Node.js 环境
- 如果没有 GitHub Token，某些功能可能受限
- 检查间隔默认 30 秒，可根据需要调整
- 最大等待时间默认 10 分钟

