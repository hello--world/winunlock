{
  "name": "winunlock",
  "version": "1.0.0",
  "description": "Windows 自动解锁凭据提供程序配置工具",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "auto-build": "node scripts/auto-build-until-success.js",
    "auto-fix": "./scripts/quick-auto-fix.sh",
    "start-auto": "./scripts/start-auto-build.sh",
    "fully-auto": "node scripts/fully-auto-build.js"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.5.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.5.0",
    "vite": "^5.0.0"
  }
}

