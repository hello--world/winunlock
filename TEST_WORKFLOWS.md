# GitHub MCP Server 工作流程功能测试

## ✅ 测试步骤

### 1. 代码已推送

刚刚推送了代码到 `dev` 分支：
- 提交 SHA: `5171e84`
- 提交信息: "添加 GitHub MCP Server 工作流程自动化文档和脚本"

### 2. 在 Cursor 中测试工作流程功能

现在你可以在 Cursor 聊天中尝试以下命令：

#### 测试 1: 列出所有工作流程
```
你: "列出 winunlock 仓库的所有工作流程"
```

预期结果：应该显示 `Build WinUnlock` 工作流程

#### 测试 2: 列出工作流程运行
```
你: "列出 Build WinUnlock 工作流程的运行"
```

或者：
```
你: "列出工作流程运行，分支为 dev"
```

预期结果：应该显示最近的运行，包括刚刚推送触发的运行

#### 测试 3: 获取最新运行详情
```
你: "获取最新工作流程运行的详情"
```

预期结果：显示运行状态、触发原因、提交 SHA 等信息

#### 测试 4: 下载运行日志（如果构建失败）
```
你: "下载最新工作流程运行的日志"
```

或者：
```
你: "下载最新失败的工作流程运行的日志"
```

预期结果：下载完整的构建日志，包含所有错误信息

### 3. 检查构建状态

等待几秒钟让 GitHub Actions 开始运行，然后：

```
你: "列出状态为进行中的工作流程运行"
你: "列出状态为失败的工作流程运行"
你: "列出状态为成功的工作流程运行"
```

## 🔍 如果功能不可用

如果上述命令在 Cursor 中无法使用，可能的原因：

1. **GitHub MCP Server 版本问题**：可能需要更新到最新版本
2. **功能名称不同**：可能需要使用不同的命令格式
3. **权限问题**：确保 GitHub PAT 有足够的权限

### 替代方案：通过 PR 检查

如果工作流程功能不可用，可以通过 PR 来检查构建状态：

```
1. 创建 PR
   你: "创建一个从 dev 到 main 的 Pull Request，标题：测试工作流程"

2. 检查 PR 状态
   你: "获取 PR #123 的状态"
   你: "检查 PR #123 的构建状态"

3. 查看 PR 详情
   你: "获取 PR #123 的 diff"
   你: "显示 PR #123 的审查评论"
```

## 📊 当前状态

- ✅ 代码已推送到 `dev` 分支
- ✅ GitHub Actions 应该已触发（根据 `.github/workflows/build.yml` 配置）
- ⏳ 等待 GitHub Actions 运行完成
- 🔍 使用上述命令检查运行状态

## 🎯 下一步

1. 在 Cursor 中尝试上述命令
2. 查看工作流程运行状态
3. 如果构建失败，下载日志并分析错误
4. 根据错误修复代码（如果需要）

---

**提示**：GitHub Actions 通常需要几分钟时间来完成构建。请等待一段时间后再检查运行状态。

